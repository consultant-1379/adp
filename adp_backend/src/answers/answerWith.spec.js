// ============================================================================================= //
/**
* Unit test for [ global.adp.Answers.answerWith ]
* @author Armando Schiavon Dias [escharm]
*/
// ============================================================================================= //
/*
describe('Using [ global.adp.Answers.answerWith ] and testing... ', () => {
  beforeEach(() => {
    global.adp = {};
    global.adp.docs = {};
    global.adp.docs.list = [];
    global.adp.AnswerClass = require('./AnswerClass'); // eslint-disable-line global-require
    global.adp.Answers = {};
    global.adp.Answers.answerWith = require('./answerWith'); // eslint-disable-line global-require
  });

  afterEach(() => {
    global.adp = null;
  });

  it('getCode() should returns 200.', () => {
    expect(global.test.answer.getCode()).toEqual(200);
  });
});
*/
// ============================================================================================= //
